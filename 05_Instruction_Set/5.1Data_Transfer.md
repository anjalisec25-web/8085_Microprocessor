# Data Transfer Instructions in 8085

## What are Data Transfer Instructions?

Data transfer instructions are used to copy data from one location to another. The source data remains unchanged after the transfer.

These instructions do not affect any flags.

---

## Common Data Transfer Instructions

| Instruction | Description | Example |
|-------------|-------------|---------|
| MOV | Copy data from one register to another | MOV A, B |
| MVI | Load immediate data into a register | MVI C, 25H |
| LDA | Load accumulator from memory | LDA 2500H |
| STA | Store accumulator into memory | STA 2501H |
| LDAX | Load accumulator indirectly | LDAX B |
| STAX | Store accumulator indirectly | STAX D |
| LHLD | Load H and L from memory | LHLD 2500H |
| SHLD | Store H and L into memory | SHLD 2500H |
| LXI | Load 16-bit data into a register pair | LXI H, 2500H |
| XCHG | Exchange HL and DE register pairs | XCHG |

---

## Example

```assembly
MVI A, 25H
MOV B, A
STA 2500H
HLT
```

### Explanation

- `MVI A,25H` → Load 25H into A.
- `MOV B,A` → Copy A to B.
- `STA 2500H` → Store A into memory location 2500H.
- `HLT` → Stop execution.